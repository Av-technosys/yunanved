import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ik.imagekit.io", "yunanved-website.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
